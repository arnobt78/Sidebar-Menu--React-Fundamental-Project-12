/** Projects route (path "/projects"). Uses shared page-content layout for consistent centered stack. */
const Projects = () => (
  <main>
    <div className="page-content">
      <h1>Projects</h1>
      <p>Educational page for projects. Use the menu to navigate.</p>
    </div>
  </main>
);
export default Projects;
