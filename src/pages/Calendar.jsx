/** Calendar route (path "/calendar"). Uses shared page-content layout for consistent centered stack. */
const Calendar = () => (
  <main>
    <div className="page-content">
      <h1>Calendar</h1>
      <p>Educational page for calendar. Use the menu to navigate.</p>
    </div>
  </main>
);
export default Calendar;
